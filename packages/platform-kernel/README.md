# @minierp/platform-kernel

## 目的

提供 platform 层的“最小公共内核”骨架（当前仅用于占位与依赖边界约束）。

## 使用

```ts
import {} from '@minierp/platform-kernel'
```

## 验证

- 构建：`bun run --filter @minierp/platform-kernel build`
- Lint：`bun run --filter @minierp/platform-kernel lint`
