# 2026-03-12 任务记录 - 库存与工作台 UI 适配

## 任务目标
1. 严格复刻库存余额页面的高保真设计（1VpfS）。
2. 修复工作台顶栏搜索框的布局不对称问题。
3. 统一全局主操作按钮的 Nordic Brutalist 风格（Terracotta #C05A3C）。

## 完成事项
- **应用库存余额 UI 改动**：
  - 更新“新增库存”按钮为铜棕色 (#C05A3C)。
  - 移除并重新按反馈优化了搜索框内的“搜索”按钮。
  - 修正了搜索输入框的 Padding 与布局语义。
- **修复工作台首页布局**：
  - 修正了 `WorkspaceHomePageScaffold` 中的 `PageHeader` 用法，通过 `actions` 属性实现搜索栏完美靠右。
  - 调整了 Header 的垂直对齐方式为 `items-center`。
- **文档同步**：
  - 更新了 `docs/page-state-ledger.md` 记录本次 UI 精修。
  - 确认 `/inventory/balance` 仍处于 `page-view` 状态，但 UI 一致性已达标。

## 待办与风险
- [ ] 为 `/inventory/balance` 补齐单元测试。
- [ ] 检查其他 legacy 页面是否仍有搜索栏不对称的问题。
- [ ] 确认所有模块的“导出”按钮是否也需要统一风格（当前保留了灰色描边风格）。

## PR 落点
- 涉及文件：
  - `apps/web/src/components/views/erp/integrated/inventory/balance/view.tsx`
  - `apps/web/src/components/views/erp/integrated/workspace/source-home/view.tsx`
  - `apps/web/src/app/(dashboard)/workspace/workspace-home-page-view.tsx`
  - `docs/page-state-ledger.md`
