# 2026-03-13

## 完成事项

- 创建 OpenSpec 变更：`canonical-erp-domain-refactor`
- 新增 canonical contract freeze 文档：`docs/architecture/erp-canonical-contract-freeze.md`
- 在 `packages/shared/src/types/erp/*` 建立 canonical ERP shared type 骨架
- 将旧 `document.ts` / `documents.ts` / `inventory.ts` / `masterdata.ts` / `entities.ts` 接上 canonical 兼容导出
- 补充 `apps/web/src/components/ui/data-table.tsx`，修复 `/integration/logs` 的构建阻塞来源
- 将采购/销售订单草稿表单从中文状态值切换到 canonical status code
- 扩展 OpenSpec 变更，补充 Prisma canonical phase 1 与 server trading baseline 的 spec / task / design
- 将 `apps/server/prisma/schema.prisma` 扩展为 additive canonical phase 1：新增 `company / org_unit / uom / tax_code / warehouse_bin / inventory_txn* / item / goods_receipt* / shipment*`，并补齐 legacy 表扩展字段
- 新增 server trading catalog，并让 purchase / sales / inbound / outbound boundary 复用 shared canonical 状态源
- 生成并审阅 `20260313113000_canonical_erp_phase1_additive` migration，随后已对远程 `192.168.1.68:5432/minierp` 执行 `prisma migrate deploy`

## 验证

- 已执行：`bun run --filter web build`
  - 结果：通过
  - 备注：仍有 4 条现存 ESLint warning（未使用变量），但不阻塞 build
- 已执行：`bun run --filter server test`
  - 结果：通过
  - 明细：40 suites / 265 tests 全部通过
- 已执行：`bunx prisma validate --schema ./prisma/schema.prisma`
  - 结果：通过
- 已执行：`bunx prisma migrate status --schema ./prisma/schema.prisma`
  - 结果：远程 `192.168.1.68:5432/minierp` schema up to date
- 已执行：`bun run db:migrate`（在 `apps/server`）
  - 结果：远程数据库已成功应用 `20260313113000_canonical_erp_phase1_additive`

## 待办

- 将 server 主写路径逐步从 `DocumentsService` 拆到正式 trading 模块，优先处理 `purchase_order / goods_receipt / sales_order / shipment`
- 将 BFF fixture/meta 补字段逻辑逐步改成 canonical null/compat alias 策略
- 将客户/供应商/仓库/税码等表单从裸文本输入切到 lookup selector
- 为新的 canonical 表补 repository / service / controller，而不是仅停留在 schema 可用状态

## 风险

- 远程库已具备 canonical phase 1 表结构，但当前主写路径仍主要落在 legacy 表
- shared canonical 类型和 server trading catalog 已冻结状态源，但 BFF / Web 还未全面切到 canonical 资源
- 兼容窗口内新旧命名并存，若不继续推进主路径切换，仍可能再次漂移
