# 2026-03-13

## 完成事项

- 创建 OpenSpec 变更：`canonical-erp-domain-refactor`
- 新增 canonical contract freeze 文档：`docs/architecture/erp-canonical-contract-freeze.md`
- 在 `packages/shared/src/types/erp/*` 建立 canonical ERP shared type 骨架
- 将旧 `document.ts` / `documents.ts` / `inventory.ts` / `masterdata.ts` / `entities.ts` 接上 canonical 兼容导出
- 补充 `apps/web/src/components/ui/data-table.tsx`，修复 `/integration/logs` 的构建阻塞来源
- 将采购/销售订单草稿表单从中文状态值切换到 canonical status code
- 扩展 OpenSpec 变更，补充 Prisma canonical phase 1 与 server trading baseline 的 spec / task / design
- 将 `apps/server/prisma/schema.prisma` 扩展为 additive canonical phase 1：新增 `company / org_unit / uom / tax_code / warehouse_bin / inventory_txn* / item / goods_receipt* / shipment*`，并补齐 legacy 表扩展字段
- 新增 server trading catalog，并让 purchase / sales / inbound / outbound boundary 复用 shared canonical 状态源
- 生成并审阅 `20260313113000_canonical_erp_phase1_additive` migration，随后已对远程 `192.168.1.68:5432/minierp` 执行 `prisma migrate deploy`
- 新建 OpenSpec change：`trading-write-path-extraction`
- 将 `DocumentsService` 的 persisted create/action 写路径委托给正式 trading services：`PurchaseInboundWriteService` 与 `SalesShipmentWriteService`
- 保留 `/documents` 兼容 façade、幂等缓存与 in-flight dedup，不改变 controller/BFF API 形状
- 新增 focused delegation test，确认 persisted 写路径不再直接堆在 `DocumentsService`
- 新建 OpenSpec change：`trading-read-model-extraction`
- 将 persisted `PO / GRN / SO / OUT` 的 `list/getDetail` 读路径委托给正式 trading read model service：`TradingDocumentsReadService`
- 保留 `/documents` controller 与兼容返回形状不变；`ADJ` 与 demo/in-memory 路径继续留在 `DocumentsService`
- 新增 focused delegation test，确认 persisted 读路径不再由 `DocumentsService` 直接实现
- 新建 OpenSpec change：`documents-facade-compatibility-shrink`
- 删除 `DocumentsService` 中已被 trading read/write services 替代的 persisted 私有 helper，实现边界收缩为 façade + compatibility store
- 新增 focused fallback test，确认 `ADJ` 列表继续走 façade 本地 demo store，而不是委托 trading read service
- 新建 OpenSpec change：`bff-canonical-resource-switch`
- 在 BFF 暴露 canonical 顶层资源：`/api/bff/items`、`/api/bff/purchase-orders`、`/api/bff/sales-orders`
- 保留 `/api/bff/skus`、`/api/bff/mdm/items`、`/api/bff/procure/purchase-orders`、`/api/bff/sales/orders` 作为 compatibility alias
- 将 SKU、采购订单、销售订单页面 hooks 与 mutation endpoint 切到 canonical 顶层 BFF 资源
- 抽出 `apps/web/src/app/api/bff/_shared/trading-order-mappers.ts`，统一采购/销售订单的 status、list、detail 映射
- 让采购/销售 canonical route 与 compatibility alias 继续复用同一套 mapper，而不是在各 route 内散落重复转换逻辑
- 为采购/销售 canonical list row 补上稳定 `id`，同时保留 `po/so` 作为展示单号字段
- 为采购/销售 `[id]` detail route 增加 upstream fallback：本地 draft 未命中时，可继续读取 backend `/documents/PO|SO/{id}` 并映射成表单 detail payload
- 编辑态 detail preload 现在优先用稳定 `id` 请求 detail；upstream detail 的缺失 counterparty 字段继续由当前列表行 fallback 补齐
- 扩展 server/shared document detail 契约：读路径现在会带出 `counterpartyId`、`supplierId/customerId` 与行级 `itemNameSnapshot/specModelSnapshot/uom`
- BFF canonical order detail mapper 现在优先使用 upstream `supplierId/customerId` 和行快照标签，页面只在 upstream 仍缺名称时保留 fallback label
- 新增 `masterdata-detail-resolvers.ts`，在 persisted `PO/SO` detail 命中后补查 `/suppliers/:id` 或 `/customers/:id`，优先返回 `code · name` 作为 lookup label
- 为 `/api/bff/customers/[id]`、`/api/bff/suppliers/[id]` 新增 GET；`mdm/customers/[id]` 与 `mdm/suppliers/[id]` 改成纯 alias re-export
- 在 `masterdata-detail-resolvers.ts` 中补充 item detail 解析，并让采购/销售 persisted detail 在缺少 `itemNameSnapshot` 时自动补全 `code · name` 行标签
- 将 `customers/suppliers/items` 的 canonical detail 读取统一为共享 result model，订单 detail enrichment 与 detail GET route 现在复用同一套 backend/fixture fallback 逻辑
- 将客户/供应商列表 hook、create/update/delete mutation 切到 canonical `/customers`、`/suppliers`，并为编辑弹窗补上 detail preload
- 将 `mdm/skus` 编辑弹窗切到 canonical `/items/:id` detail preload，打开编辑态时先 fallback 再以 item detail 覆盖
- 将 SKU/Item 表单与 BFF payload 扩到 richer canonical item 字段：`barcode / batchManaged / serialManaged / minStockQty / maxStockQty / leadTimeDays`
- 将 server masterdata 的 item/sku 主写链补到 richer 字段：domain、controller、service、repository 现在都能承接 `barcode / batchManaged / serialManaged / minStockQty / maxStockQty / leadTimeDays`
- 继续把 item canonical 字段面向前推进到 `itemType / taxRate / shelfLifeDays`，并同步进 shared、server、BFF、SKU 表单与预览
- 新建 OpenSpec change：`order-form-lookup-selectors`
- 新增共享远程 lookup 组件：`remote-entity-select.tsx`
- 将采购订单表单的 `supplierId` 文本输入切为 `/api/bff/suppliers` 驱动的 lookup selector
- 将销售订单表单的 `customerId` 文本输入切为 `/api/bff/customers` 驱动的 lookup selector
- 将采购/销售订单表单从 header-only 草稿输入升级为 `header + lines`，行项目使用 `/api/bff/items` lookup
- 更新采购/销售 BFF draft route 与 `_store`，按行项目计算 `amount / skuCount` 并保留 counterparty label
- 为采购/销售订单新增 BFF detail GET route，本地 draft 编辑态可回填真实 `header + lines`
- 将采购/销售列表页编辑弹窗改为“先 fallback、再异步覆盖 detail”，只有 legacy/upstream 摘要行继续使用兼容摘要行

## 验证

- 已执行：`bun run --filter web build`
  - 结果：通过
  - 备注：仍有 4 条现存 ESLint warning（未使用变量），但不阻塞 build
- 已执行：`bun run --filter server test`
  - 结果：通过
  - 明细：42 suites / 270 tests 全部通过
- 已执行：`bunx prisma validate --schema ./prisma/schema.prisma`
  - 结果：通过
- 已执行：`bunx prisma migrate status --schema ./prisma/schema.prisma`
  - 结果：远程 `192.168.1.68:5432/minierp` schema up to date
- 已执行：`bun run db:migrate`（在 `apps/server`）
  - 结果：远程数据库已成功应用 `20260313113000_canonical_erp_phase1_additive`

## 待办

- 将 `DocumentsService` 中剩余的 persisted 兼容逻辑继续收缩到 façade，只保留 demo / `ADJ` / fallback 路径
- 将 BFF fixture/meta 补字段逻辑逐步改成 canonical null/compat alias 策略
- 将客户/供应商/仓库/税码等表单从裸文本输入切到 lookup selector
- 为新的 canonical 表补 repository / service / controller，而不是仅停留在 schema 可用状态
- 将采购、销售、物料 BFF route 内部从 legacy upstream 和页面特化 DTO 继续收敛到更稳定的 canonical mapper
- 推进客户、供应商、仓库、税码、item 的 lookup selector 和 canonical form payload
- 为编辑态补齐真实 customer/supplier id 回填，避免列表行展示名继续被当作 fallback value
- 将采购/销售 BFF 内部 mapper 从 legacy upstream 摘要继续收敛到更稳定的 canonical detail shape
- 将采购/销售 upstream detail 接到新的共享 mapper，替换当前仅本地 draft 具备 detail 的状态
- 评估是否要把 `PurchaseOrderListItem / SalesOrderListItem` 进一步迁移出 mock fixtures，沉淀到更稳定的 shared/web VM 契约
- 让 upstream detail 不只带真实 id，还能通过 masterdata lookup 或 server join 带出真实 counterparty 名称，彻底删除 edit form 的名称 fallback
- 评估是否给 BFF `customers/[id]`、`suppliers/[id]` 补 GET alias，避免 detail route 继续直接请求 backend masterdata path
- 将订单 detail 的 masterdata 名称解析改成通过 BFF canonical detail helper/route 复用，而不是继续走直连 backend fetch
- 继续减少 shared resolver 对 backend 直连 fetch 的依赖，评估是否让订单 detail enrichment 只消费 canonical BFF detail/service
- 评估是否继续把 canonical detail resolver 向更完整的 masterdata service 收敛，并补充 route 级测试锁定 fallback 行为
- 继续把 MDM 页面从 legacy `mdm/*` 路径收敛到顶层 canonical 资源，优先处理 item/SKU 编辑态详情与更完整字段回填
- 继续推进 SKU/Item 表单字段扩展，逐步从当前轻量字段集向 canonical item 字段面靠拢
- 继续评估是否要把 tax code、item type、shelf life 等字段接入 item 表单，以及对应 lookup/resource 的承接方式
- 继续把 richer item 字段从当前 server 持久化扩到 item/sku controller spec、BFF detail 和更完整的 canonical item DTO
- 下一步优先考虑 `taxCodeId`、`uom/tax-code` lookup 和 item canonical DTO 完整输出，而不是继续只在页面新增孤立字段

## 风险

- 远程库已具备 canonical phase 1 表结构，但当前主写路径仍主要落在 legacy 表
- shared canonical 类型、server trading catalog 以及 `/documents` 的 persisted 读写边界已收敛到 façade + trading services，BFF 顶层 canonical route 也已暴露，但内部 mapper 和页面表单仍未完全 canonical 化
- BFF 的采购/销售 list/detail/status 映射已开始集中到共享 mapper，但 upstream 明细仍未真正接入，canonical detail 目前主要覆盖本地 draft
- BFF 的采购/销售 list row 已具备稳定 id，upstream detail 也已接入共享 mapper，但 upstream detail 仍不含真实 counterparty id/name，需要继续消除 fallback 依赖
- upstream detail 已带出真实 counterparty id 和行快照，但 persisted detail 仍缺人类可读 counterparty 名称；当前编辑态会保留列表行 fallback label 以维持可用性
- persisted detail 已可通过 masterdata detail 补出大部分真实 counterparty 名称；剩余风险主要是 lookup 失败时仍回退到列表行 fallback
- canonical customer/supplier detail 路由已具备 GET，但 masterdata 名称解析目前仍复用 shared backend helper；后续可继续向“订单 detail 仅依赖 BFF canonical 资源”收敛
- persisted order line 在缺少行快照时已可通过 canonical item detail 补出更稳定的 `code · name`；剩余风险主要是 lookup 失败时仍会回退到旧占位标签
- canonical customer/supplier/item detail 已共享相同读取规则；剩余风险主要是这层尚未有独立 route 级测试锁定 fixture/not-found 分支
- 客户/供应商页面已切 canonical 顶层资源，但编辑态详情仍受当前上游 detail 字段深度限制，信用额度等扩展字段继续依赖 fallback
- SKU 编辑态已接 canonical item detail，但当前 item detail 字段仍偏轻量，供应商/仓库/批次等 richer canonical 字段还没有进入表单
- richer item 字段已进入 web/BFF 契约，但上游 server 当前仍只对基础 SKU 字段做正式持久化；新增字段主要用于 canonical 页面契约和 fallback/detail 展示
- richer item 字段现在已进入 server 持久化主链，但 item canonical DTO 与上游消费面还没有全面切换到这些字段
- `itemType / taxRate / shelfLifeDays` 已进入当前主链，但 `taxCodeId` 仍未接入 lookup 资源，税务字段还没完全对齐 canonical contract
- 采购/销售表单已切 counterparty lookup，但编辑态仍受限于当前列表行缺少真实 counterparty id，只能临时注入 fallback 当前值
- 采购/销售表单对本地 draft 已可回填真实 `header + lines`，但 legacy/upstream 列表行仍会回退到“兼容摘要行”兜底
- 兼容窗口内新旧命名并存，若不继续推进主路径切换，仍可能再次漂移
